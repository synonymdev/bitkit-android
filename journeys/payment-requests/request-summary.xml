<journey name="Payment Request Summary">
  <description>
    Verifies that the collapsed Payment Request confirmation shows who the request is from and what it is for, keeps the note in the details, and leaves For out when the request has no note. Requires two Bitkit instances saved as each other's contacts and linked on receiver path "bitkit/wallet", with the payer funded to cover 21,000 sats; see README.md.
  </description>
  <actions>
    <action>On the requester, send the payer contact a Payment Request for 21,000 sats with the note "Lunch last week"</action>
    <action>On the payer, verify the Payment Request confirmation screen (testTag "PaymentRequestConfirm") appears with 21,000 sats</action>
    <action>Verify From (testTag "PaymentRequestFrom") shows the requester's contact name</action>
    <action>Verify For (testTag "PaymentRequestFor") shows "Lunch last week"</action>
    <action>Tap Show details (testTag "SendConfirmToggleDetails")</action>
    <action>Verify From (testTag "PaymentRequestFrom") and For (testTag "PaymentRequestFor") are no longer shown, and the recipient (testTag "ReviewContactRecipient") under Contact is the requester's contact</action>
    <action>Verify the invoice note (testTag "PaymentRequestInvoiceNote") shows "Lunch last week"</action>
    <action>Close the Payment Request confirmation screen without paying</action>
    <action>On the requester, send the payer contact a Payment Request for 5,000 sats with no note</action>
    <action>On the payer, verify the Payment Request confirmation screen (testTag "PaymentRequestConfirm") shows 5,000 sats, opening it from the pending Payment Requests bell (testTag "PaymentRequestsBell") if it does not open by itself</action>
    <action>Verify From (testTag "PaymentRequestFrom") shows the requester's contact name and For (testTag "PaymentRequestFor") is not shown</action>
  </actions>
</journey>
