<journey name="Hardware Wallet Activity Tags And Inputs Outputs">
  <description>
    Verifies that a hardware-wallet transaction behaves as a first-class bitkit-core activity:
    its detail screen supports tags (which persist and keep the item visible under a tag
    filter) and its Explore screen shows the transaction inputs and outputs fetched from the
    configured Electrum backend. Requires a paired Bridge emulator with the mixed hardware
    activity fixture from README.md. Use a hardware seed distinct from the Bitkit wallet seed
    so the transaction resolves as a hardware (blue-icon) activity, not a local one.
  </description>
  <actions>
    <action>
      Launch the Bitkit app and go to the wallet home screen
    </action>
    <action>
      Tap "Show All" beneath the activity list
    </action>
    <action>
      In All Activity, tap a blue hardware received activity row with a distinct amount, not the Transfer / From Savings row
    </action>
    <action>
      Verify an activity detail screen opens showing a blue icon, a received on-chain amount, and status content for that selected hardware transaction
    </action>
    <action>
      Tap "Add Tag", enter the tag "hwtest" and confirm it
    </action>
    <action>
      Verify a tag chip labelled "hwtest" is shown on the activity detail screen
    </action>
    <action>
      Navigate back to All Activity, then tap the same blue hardware received activity again and verify the "hwtest" tag is still shown (it persisted to bitkit-core)
    </action>
    <action>
      Tap "Explore", verify the Activity Explorer screen opens and shows an "Inputs" section and an "Outputs" section each listing at least one entry
    </action>
    <action>
      Navigate back to All Activity
    </action>
    <action>
      Open the tag filter, select the "hwtest" tag, and verify only the tagged blue hardware activity remains listed from the hardware fixture; the other untagged blue received or transfer rows should not match the tag filter
    </action>
  </actions>
</journey>
