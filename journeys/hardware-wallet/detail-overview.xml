<journey name="Hardware Wallet Detail Overview">
  <description>
    Opens the hardware wallet detail screen from the home tile and verifies its overview:
    the top bar, balance header, the Transfer To Spending entry when funds are present,
    the grouped activity list, and the Remove device confirm dialog. The final
    Remove step forgets the device, so run this last (re-run connect-home-tile.xml to pair
    again). Requires a paired Bridge emulator (run connect-home-tile.xml first) and the mixed
    hardware activity fixture from README.md, so this journey verifies multiple rows instead
    of a single hardware transaction.
  </description>
  <actions>
    <action>
      Launch the Bitkit app and go to the wallet home screen
    </action>
    <action>
      Tap the hardware wallet tile beneath the SAVINGS and SPENDING tiles
    </action>
    <action>
      Verify the hardware wallet detail screen opens (testTag "HardwareWalletScreen"), showing the device name with a blue bitcoin icon in the top bar and a balance header
    </action>
    <action>
      If a "Transfer To Spending" button is shown (testTag "HardwareTransferToSpending"), tap it; if the first-run Transfer To Spending intro appears, tap "Get Started"; verify the transfer amount screen opens (testTag "HardwareTransferAmount") titled "TRANSFER TO SPENDING", then navigate back to the hardware wallet detail screen; otherwise skip this step
    </action>
    <action>
      Verify the detail activity list shows multiple blue hardware rows for the paired wallet, including at least one received row and one Transfer / From Savings row, and does not show a duplicate normal TransferIcon row for the same hardware transfer amount
    </action>
    <action>
      Tap one blue hardware row, verify an activity detail screen opens with a blue icon, then navigate back
    </action>
    <action>
      Tap the "Remove" button labelled with the device name near the bottom of the screen (testTag "RemoveHardwareWallet")
    </action>
    <action>
      Verify a confirm dialog appears explaining that your funds are safe and your coins won't be deleted
    </action>
    <action>
      Tap "Cancel" and verify the hardware wallet detail screen is still shown
    </action>
    <action>
      Tap the "Remove" button labelled with the device name again, then tap "Remove" in the confirm dialog
    </action>
    <action>
      Verify the app returns to the wallet home screen and no hardware wallet tile is shown
    </action>
  </actions>
</journey>
