<journey name="Hardware Wallet Transfer To Spending">
  <description>
    Drives the watch-only Transfer To Spending flow for a paired Trezor: Amount -> Sign With
    Your Device -> Transaction Signed -> Processing Payment -> same-backend confirmation ->
    activity verification. The funding send is signed on the Bridge emulator device, so no
    physical Trezor is required. Requires a paired Bridge emulator (run connect-home-tile.xml
    first) whose native-segwit account holds spendable regtest funds on the staging Blocktank
    regtest backend. Run with the mixed hardware activity fixture from README.md so the final
    Activity assertions prove the new transfer coexists with older hardware receive rows and is
    not duplicated as a default-wallet activity. The journey mines the funding transaction from
    the app's Blocktank Regtest screen before checking the confirmed state. Mirrors the on-chain
    Transfer To Spending flow, swapping local signing for the device.
  </description>
  <actions>
    <action>
      Launch the Bitkit app and go to the wallet home screen
    </action>
    <action>
      Before starting, verify the home or hardware wallet detail Activity list already contains at least one blue hardware received row with a distinct amount; this is the prior-history control used to detect ordering and filtering regressions after the new transfer
    </action>
    <action>
      Tap the hardware wallet tile beneath the SAVINGS and SPENDING tiles, and verify the hardware wallet detail screen opens (testTag "HardwareWalletScreen")
    </action>
    <action>
      Verify the hardware wallet has a non-zero spendable balance
    </action>
    <action>
      Tap the "Transfer To Spending" button (testTag "HardwareTransferToSpending")
    </action>
    <action>
      If the first-run Transfer To Spending intro appears, tap "Get Started"
    </action>
    <action>
      Verify the transfer amount screen opens (testTag "HardwareTransferAmount"), titled "TRANSFER TO SPENDING", showing an AVAILABLE row, the 25% and MAX quick buttons, and a number pad
    </action>
    <action>
      Tap the "25%" quick button (testTag "HardwareTransferAmountQuarter") to set a valid amount below the available limit
    </action>
    <action>
      Tap "Continue" (testTag "HardwareTransferAmountContinue") and wait for the Blocktank order to be created
    </action>
    <action>
      Verify the sign screen opens (testTag "HardwareTransferSign"), titled "SIGN WITH YOUR DEVICE", showing the NETWORK FEES, SERVICE FEES, TO SPENDING and TOTAL cells, the Learn More and Advanced buttons, and the Trezor illustration
    </action>
    <action>
      Tap "Open Trezor Connect" (testTag "HardwareTransferOpenTrezorConnect")
    </action>
    <action>
      On the Bridge emulator, promptly approve the Recipient #1, Amount #1, Confirm Locktime and Summary prompts in that order, completing all four within 120 seconds
    </action>
    <action>
      Immediately verify the one-second transaction signed handoff appears (testTag "HardwareTransferSigned"), titled "TRANSACTION SIGNED", showing the same fee cells and the checkmark illustration
    </action>
    <action>
      Wait for the screen to auto-forward and verify the Processing Payment / setting-up progress screen appears as an intermediate state, not as the final success condition
    </action>
    <action>
      Before mining, tap "Continue Using Bitkit" (testTag "TransferSuccess-button") and verify the app returns to the wallet home screen
    </action>
    <action>
      Before mining, verify the recent Activity list contains exactly one new hardware wallet transfer row for the new tx, with the blue hardware icon, title "Transfer", subtitle "From Savings", and a non-confirmed/non-stuck transfer status, above the prior blue received row from the fixture
    </action>
    <action>
      Before mining, open the new hardware transfer row and verify the Activity detail shows "TO SPENDING" and a pending or otherwise non-confirmed transfer status, then navigate back to the wallet home screen
    </action>
    <action>
      Open the main menu from the wallet home screen
    </action>
    <action>
      Tap "Settings"
    </action>
    <action>
      Tap "Dev Settings" (testTag "DevSettings")
    </action>
    <action>
      Scroll to the REGTEST section and tap "Blocktank Regtest"
    </action>
    <action>
      Verify the "Blocktank Regtest" screen is shown and the block count is set to 1
    </action>
    <action>
      Tap "Mine Blocks"
    </action>
    <action>
      Verify the success message says "Successfully mined 1 blocks"
    </action>
    <action>
      Tap the top-bar back button to return to Dev Settings
    </action>
    <action>
      Tap the top-bar back button to return to Settings
    </action>
    <action>
      Tap the top-bar back button to return to the wallet home screen
    </action>
    <action>
      Verify the app/Core sync observes the newly mined confirmation
    </action>
    <action>
      Verify the SPENDING balance has increased by the transfer amount and the same hardware wallet transfer row for the new tx is now confirmed
    </action>
    <action>
      Verify the new transfer amount is not duplicated as a second default-wallet row with a normal TransferIcon or the same Transfer / From Savings text
    </action>
    <action>
      Open the same hardware transfer row again and verify the Activity detail shows "TO SPENDING" and status "Confirmed"
    </action>
  </actions>
</journey>
