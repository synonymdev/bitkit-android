<journey name="Hardware Wallet Transfer To Spending">
  <description>
    Drives the watch-only Transfer To Spending flow for a paired Trezor: Amount -> Sign With
    Your Device -> Transaction Signed -> Processing Payment, then verifies the new transfer is
    represented once in the hardware wallet's Core activity scope. Requires a paired Bridge
    emulator whose native-segwit account holds spendable regtest funds and at least one older
    hardware receive row with a distinct amount.
  </description>
  <actions>
    <action>
      Launch the Bitkit app and go to the wallet home screen
    </action>
    <action>
      Verify the recent activity list contains an older blue hardware received row with a distinct amount
    </action>
    <action>
      Tap the hardware wallet tile beneath the SAVINGS and SPENDING tiles, and verify the hardware wallet detail screen opens (testTag "HardwareWalletScreen")
    </action>
    <action>
      Tap the "Transfer To Spending" button (testTag "HardwareTransferToSpending")
    </action>
    <action>
      If the first-run Transfer To Spending intro is shown, tap "Get Started"
    </action>
    <action>
      Verify the transfer amount screen opens (testTag "HardwareTransferAmount"), titled "TRANSFER TO SPENDING", showing an AVAILABLE row, the 25% and MAX quick buttons, and a number pad
    </action>
    <action>
      Tap the "25%" quick button (testTag "HardwareTransferAmountQuarter") to set a valid amount below the available limit
    </action>
    <action>
      Tap "Continue" (testTag "HardwareTransferAmountContinue") and wait for the Blocktank order to be created
    </action>
    <action>
      Verify the sign screen opens (testTag "HardwareTransferSign"), titled "SIGN WITH YOUR DEVICE", showing the NETWORK FEES, SERVICE FEES, TO SPENDING and TOTAL cells, the Learn More and Advanced buttons, and the Trezor illustration
    </action>
    <action>
      Tap "Open Trezor Connect" (testTag "HardwareTransferOpenTrezorConnect")
    </action>
    <action>
      Approve the Recipient, Amount, Locktime, and Summary prompts on the Bridge emulator in order
    </action>
    <action>
      Verify the transaction signed screen appears (testTag "HardwareTransferSigned"), titled "TRANSACTION SIGNED", showing the same fee cells and the checkmark illustration
    </action>
    <action>
      Wait for the screen to auto-forward and verify the Processing Payment / setting-up progress screen appears
    </action>
    <action>
      Tap "Continue Using Bitkit" and verify the app returns to the wallet home screen
    </action>
    <action>
      Verify the recent activity list contains exactly one new blue hardware Transfer row with subtitle "From Savings", above the older blue received row
    </action>
    <action>
      Tap the new blue Transfer row and verify its detail screen shows "TO SPENDING"
    </action>
  </actions>
</journey>
