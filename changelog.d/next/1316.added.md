Prepared an optional Receive Offline checkbox for Lightning invoices with an experimental ldk-node offline receive provider. The provider is compiled only with a local ldk-node build and selected only by a dev toggle, so the checkbox stays hidden in production builds until the native support ships and device validation completes.
